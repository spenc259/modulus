git config --local user.name "[enter username]"
git config --local user.email "[enter email]"

git clone [repo url]

git status

git remote
git remote -v (full location)
git remote add [remote name] [remote url]


git add * (add all files to local branch)
git commit -m "[commit message]" (commits to local branch)
git push [remote name] [remote branch]

git diff

git branch [branch name]
git checkout branch

git log

alias graph="git log --all --decorate --oneline --graph"
graph